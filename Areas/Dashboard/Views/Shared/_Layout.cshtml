<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>@ViewData["Title"] - Fitness Management System</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />

    <style>
        html, body {
            height: 100%;
            margin: 0;
            font-family: 'Segoe UI', sans-serif;
            display: flex;
            flex-direction: column;
        }

        /* Make the layout fill the viewport */
        main {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        /* Hero Section (Top Image Header) */
        .hero-section {
            background: url('https://images.pexels.com/photos/1954524/pexels-photo-1954524.jpeg?auto=compress&cs=tinysrgb&w=1600') no-repeat right center;
            background-size: cover;
            height: 35vh;
            position: relative;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
        }

        .hero-overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.55);
        }

        .hero-text {
            position: relative;
            z-index: 2;
        }

            .hero-text h1 {
                font-size: 2.5rem;
                font-weight: bold;
            }

            .hero-text p {
                font-size: 1.1rem;
            }

        /* Footer always at bottom */
        footer {
            background-color: #212529;
            color: #ccc;
            text-align: center;
            padding: 12px;
            font-size: 0.9rem;
            margin-top: auto;
        }
    </style>
</head>

<body>
    <main>
        <!-- Header -->
        <section class="hero-section">
            <div class="hero-overlay"></div>
            <div class="hero-text">
                <h1>Welcome to Fitness Management System</h1>
                <p>"Discipline is doing what needs to be done, even when you don’t want to." 💪</p>
            </div>
        </section>

        <!-- Page Body (Sidebar + Content) -->
        <div class="container-fluid flex-grow-1 my-4">
            @RenderBody()
        </div>
    </main>

    <!-- Footer -->
    <footer>
        &copy; @DateTime.Now.Year - Fitness Management System | Stay Fit, Stay Strong 🏋️‍♀️
    </footer>

    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
